export enum InterfaceState {
  BUYING = 'BUYING',
  EXTENDING = 'EXTENDING',
  WITHDRAWING = 'WITHDRAWING',
  NEUTRAL = 'NEUTRAL',
  LOADING = 'LOADING',
}

export enum ChosenLimit {
  Custom,
  MaxPosition,
  Recommended,
}
